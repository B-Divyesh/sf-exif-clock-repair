{
  "name": "exif-clock-repair",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test",
    "verify:xmp": "vite-node scripts/verify-xmp.ts",
    "typecheck": "tsc -b --pretty false",
    "lint": "tsc -b --pretty false",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "1.58.2",
    "@types/node": "^22.18.0",
    "typescript": "~5.7.3",
    "vite": "~6.4.3",
    "vitest": "~3.2.7"
  },
  "overrides": {
    "playwright-core": "1.58.2"
  }
}
